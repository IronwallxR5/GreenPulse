import prisma from '../config/prisma';
import { ImpactType } from '@prisma/client';
import { CreateImpactDTO, UpdateImpactDTO } from '../utils/interfaces';

// Repository Layer: Handles all database operations (Data Access Layer)
class ImpactRepository {
  // TODO: Implement CRUD methods
  // - create(data, userId)
  // - findById(id)
  // - findByUserId(userId, filters?)
  // - update(id, data)
  // - delete(id)
}

export default ImpactRepository;
