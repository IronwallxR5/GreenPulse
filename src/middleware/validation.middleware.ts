import { Request, Response, NextFunction } from 'express';
import { z } from 'zod';

// Validation Middleware: Use Zod for request validation
// TODO: Create validation schemas and middleware
// - validateImpactCreate
// - validateImpactUpdate
// - validateRegister
// - validateLogin
